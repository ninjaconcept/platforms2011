%h1 New <%= singular_table_name %>

= simple_form_for(@<%= singular_name %>, :url => collection_path) do |f|
  = # render 'layouts/error_messages'
  
  = render 'form', :f => f
  
  .actions
    = f.submit 'Create'
    or
    = link_to 'cancel', collection_path
