%h1 New member_of_series

= simple_form_for(@member_of_series, :url => collection_path) do |f|
  = # render 'layouts/error_messages'
  
  = render 'form', :f => f
  
  .actions
    = f.submit 'Create'
    or
    = link_to 'cancel', collection_path
