%h1 Editing series

= simple_form_for(@series, :url => resource_path(@series)) do |f|
  = render 'layouts/error_messages'
  
  = render 'form', :f => f
  
  .actions
    = f.submit 'Update'
    or
    = link_to 'back', collection_path
