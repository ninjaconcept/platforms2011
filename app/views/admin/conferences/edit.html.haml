%h1 Editing conference

= simple_form_for(@conference, :url => resource_path(@conference)) do |f|
  = render 'layouts/error_messages'
  
  = render 'form', :f => f
  
  .actions
    = f.submit 'Update'
    or
    = link_to 'back', collection_path
