%h1 Editing category

= simple_form_for(@category, :url => resource_path(@category)) do |f|
  = render 'layouts/error_messages'
  
  = render 'form', :f => f
  
  .actions
    = f.submit 'Update'
    or
    = link_to 'back', collection_path
