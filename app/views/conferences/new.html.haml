%h1 New conference

= simple_form_for(@conference, :url => collection_path) do |f|
  = # render 'layouts/error_messages'
  
  = render 'form', :f => f
  
  .actions
    = f.submit 'Create'
    or
    = link_to 'cancel', collection_path
